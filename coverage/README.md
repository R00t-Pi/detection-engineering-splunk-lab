# coverage

One file, `attack-matrix.md` — the single dashboard for "what's actually detected right now."

Every technique shows up here exactly once, whether it came from a campaign run or the
broader hardening sweep, with its current status (not started / validated / tuned). If
you ever want a fast answer to "do we detect T1003 yet," this is the file to open — not a
folder to browse.